@model IEnumerable<ClinicaMVC.Models.medicamentos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Medicamentos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                Acciones
            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tipo)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IdMedicamento }, new { @class = "btn btn-outline-info btn-xs glyphicon glyphicon-pencil" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdMedicamento }, new { @class = "btn btn-outline-success btn-xs glyphicon glyphicon-list-alt" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdMedicamento }, new { @class = "btn btn-outline-danger btn-xs glyphicon glyphicon-trash" })
                </td>

            </tr>
        }
    </tbody>
</table>
